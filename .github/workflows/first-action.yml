name: First Workflow
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: | 
          echo "E aí?"
          echo "Oi de novo!"
      - name: Print goodbye
        run: echo "Falou!"
